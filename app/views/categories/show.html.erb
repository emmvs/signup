<!-- app/views/categories/show.html.erb -->
<section class="container-show">
    <h1 class="font-alpha show__title "><%= @category.title %></h1>
      <div class="category-card--show">
        <div class="category__texts">
          <%= @category.title %>
          <%= @category.video %>
        </div>
      </div>
</section>
